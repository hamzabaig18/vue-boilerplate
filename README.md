## Getting started

```bash
# 1. Clone the repository.
git clone https://github.com/hamzabaig18/vue-boilerplate.git

# 3. Install dependencies. Make sure npm and vue cli is installed:
npm install
npm install -g @vue/cli

# 4. Start the project
node run serve
